export const REPO_SOURCE = Object.freeze({
  GITHUB: 'github',
  LOCAL: 'local',
});

export const JOB_STATUS = Object.freeze({
  QUEUED: 'queued',
  PROCESSING: 'processing',
  COMPLETED: 'completed',
  FAILED: 'failed',
});
